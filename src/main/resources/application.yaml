spring.application.name: fit-coach

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fit_coach?createDatabaseIfNotExist=true
    username: root
    password: root
  liquibase:
    enabled: true

  jpa:
    properties.hibernate:
      format_sql: true
